/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import { Jovem } from './jovem';

describe('Jovem', () => {
  it('should create an instance', () => {
    expect(new Jovem()).toBeTruthy();
  });
});
