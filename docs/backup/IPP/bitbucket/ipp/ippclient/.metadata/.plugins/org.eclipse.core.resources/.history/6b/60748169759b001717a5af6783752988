import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TeskComponent } from './tesk.component';

describe('TeskComponent', () => {
  let component: TeskComponent;
  let fixture: ComponentFixture<TeskComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TeskComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TeskComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});
